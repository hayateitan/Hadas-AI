import React from 'react'
import { Form } from 'react-bootstrap';


const Celulefilter = ({id}) => {
    return (
        <div id={'buttonFilter_' + id}>

            <Form.Select >
                <option > </option>
            </Form.Select>

        </div>
    )
}

export default Celulefilter
